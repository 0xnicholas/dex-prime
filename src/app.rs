/*
use executor::executor::Executor;
use executor::types::ExecutionRequest;
use chain_adapter::solana::SolanaAdapter;
use common_types::{ExecuteCommand, Side};
*/

/*
fn main() {
    let adapter = SolanaAdapter::new();
    let executor = Executor::new(adapter);

    let cmd = ExecuteCommand::Open {
        user: "trader1".into(),
        market: "ETH-USD".into(),
        size: 1.5,
        price: 2450.0,
        side: Side::Long,
    };

    let req = ExecutionRequest {
        command: cmd,
        dry_run: false,
    };

    let result = executor.execute(req);
    println!("Execution result: {:?}", result);
}
 */